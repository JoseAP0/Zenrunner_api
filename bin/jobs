#!/usr/bin/env ruby

require_relative "../config/environment"
require "sidekiq/cli"

cli = Sidekiq::CLI.instance
config_file = File.expand_path("../config/sidekiq.yml", __dir__)
cli.parse([ "-C", config_file, *ARGV ])
cli.run
